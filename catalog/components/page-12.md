## page-12 — Sample Pages 12 — Employment Detail Side Panel (Snapshots)  _()_
Same employment detail side panel as page-11, showing an in-review contract (V1 · Main) with the Snapshots tab active. Stage stepper: only step 1 (KA REVIEW) reached; steps 2-6 upcoming. Header action becomes a suggested next action ('Next: Edit Contract') split button. Snapshots tab: grouped snapshot timeline — 'MAIN RECORD' group strip, then snapshot rows (CR01-CR04) on a vertical dot timeline; 'REQUEST CHANGE #1' group strip below.
**Spec:** `src/components/page-12-employment-detail-snapshots.json`
