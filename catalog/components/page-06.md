## page-06 — Sample Pages 6 — Candidate Directory Table  _()_
Flat candidate directory as a wide data table with per-column filters. Same page header and list toolbar as the card grid view. Table: leading Action column with a SplitButton per row, checkbox column, photo column, then data columns whose headers embed inline search inputs (FullSearch) or inline selects (FullSelect). Horizontal scroll for overflow columns.
**Spec:** `src/components/page-06-candidate-directory.json`
